# Hotel Booking Platform - Test Cases

| Requirement Id | Test case ID | Test Case | Expected Result | Actual Result | Test Outcome |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **1.1** | 1 | The user clicks on the "Luxury" filter button on the Home Page. | The hotel list accurately updates to display only hotels categorized as "Luxury". | The list filters correctly and displays only Luxury hotels. | Passed |
| **1.2** | 2 | The user clicks on the "Tatkal Deals" filter on the Home Page. | The list updates to display only hotels where the last-minute `tatkalPrice` is lower than the regular `price`. | The list updates correctly to show available last-minute deals. | Passed |
| **1.3** | 3 | The user clicks on the "All" filter after browsing a specific category. | The hotel list resets and displays all hotels fetched from the database without any category restrictions. | The list resets correctly to show all available hotels. | Passed |
| **2.1** | 4 | The user clicks on a specific Hotel card to view details. | The application navigates to the Hotel Details page, successfully fetching and displaying the hotel's amenities, location, and associated rooms from the database. | The Hotel Details page renders correctly with the specific location, amenities, and room choices fetched from the database. | Passed |
| **2.2** | 5 | The user attempts to select a hotel that has `availability: 0` (Sold Out). | The "Book Now" button on the Home Page is disabled and visually greyed out, preventing navigation or indicating it cannot be booked. | The button is correctly disabled and displays "Sold Out". | Passed |
| **3.1** | 6 | On the Hotel Details page, the user clicks the "⚡ Fast-Track: Apply Tatkal (Today)" button. | The check-in date input is automatically populated with today's date, and the room prices dynamically update to reflect the cheaper Tatkal rates. | The date updates immediately and the discounted prices are applied to the UI displaying the "TATKAL DEAL" tag. | Passed |
| **4.1** | 7 | The user selects a room and a date, and presses the "Confirm Booking" button while NOT logged into the system. | A warning alert is displayed prompting the user to log in, and the user is redirected to the `/login` route. | The alert is correctly triggered and the navigation redirects the user to the login screen. | Passed |
| **4.2** | 8 | The user presses "Confirm Booking" without selecting a check-in date. | An error message "Please select dates and a room" is displayed to the user, preventing further action. | The error message is successfully displayed to the user. | Passed |
| **4.3** | 9 | The user presses "Confirm Booking" without selecting a specific room type from the list. | An error message "Please select dates and a room" is displayed to the user, preventing the booking confirmation. | The error message is successfully displayed to the user. | Passed |
| **4.4** | 10 | The logged-in user successfully completes a booking with valid dates and room selection. | The application adds the booking to the global context and navigates the user to the `/invoice` screen to view the generated bill. | The user is redirected to the invoice generation screen with correct booking details. | Passed |
| **5.1** | 11 | The user navigates to the Curated Packages screen (`/packages`). | A grid of custom travel packages is fetched from the database and displayed with their respective inclusions and prices. | Packages are fetched and rendered properly without errors. | Passed |
| **6.1** | 12 | The user enters valid credentials on the Login screen and submits. | The system authenticates the user, updates the global session state to logged in, and redirects the user to their intended destination (or home). | Authentication is successful and the user context updates. | Passed |
| **6.2** | 13 | The user enters invalid credentials on the Login screen. | An "Invalid username or password" error is displayed, and access is denied. | Error safely prevents login and displays correctly. | Passed |
| **7.1** | 14 | The user views the Invoice Screen after a successful booking. | A formatted bill is displayed showing the booking details, room pricing (including Tatkal discounts if applicable), taxes ($45), and the correct total amount. | The invoice mathematically correctly adds the room price and standard fees. | Passed |
| **8.1** | 15 | The user navigates directly to a non-existent hotel ID via URL (e.g. `/hotel/9999`). | The system gracefully handles the "not found" state and displays "Hotel not found" text instead of crashing. | The empty state handles the null fetch response successfully. | Passed |

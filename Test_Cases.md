# Hotel Booking Platform - Test Cases

| Requirement Id | Test case ID | Test Case | Expected Result | Actual Result | Test Outcome |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **1. Authentication & Roles** | | | | | |
| **1.1** | 1 | The user enters valid credentials on the Login screen and submits (Sign In Mode). | The system authenticates the user via Supabase, updates session state to logged in, and redirects the user to their intended destination (or home). | Authentication is successful and the user context updates. | Passed |
| **1.2** | 2 | The user enters invalid credentials on the Login screen. | An "Invalid login credentials" error is displayed from Supabase, and access is denied. | Error safely prevents login and displays correctly. | Passed |
| **1.3** | 3 | The user attempts to sign up with an invalid email format (e.g. `user@com`). | The browser form validation should prevent submission and alert the user. | The HTML5 validation correctly stops form submission. | Passed |
| **1.4** | 4 | The user clicks "Don't have an account? Sign up" on the Login screen. | The Auth mode toggles to Sign Up, the button text updates to "Sign Up", and the password label updates to "Create Password". | The UI conditionally renders the Create form correctly. | Passed |
| **1.5** | 5 | The user clicks "Dummy Login as Admin" on the Auth page. | The system mocks the Admin login, updates `user` context to an admin role, and navigates to `/`. | The Admin mock login works and updates the Navbar with "Admin Panel". | Passed |
| **1.6** | 6 | The user clicks "Dummy Login as Management" on the Auth page to access the Admin Panel. | The Navbar should display the "Admin Panel" link for the management role as well. | The Navbar only shows the "Admin Panel" if the role is 'admin', hiding it from 'management'. | Failed |
| **2. Home Page & Filters** | | | | | |
| **2.1** | 7 | The user clicks on the "Luxury" filter button on the Home Page. | The hotel list accurately updates to display only hotels categorized as "Luxury". | The list filters correctly and displays only Luxury hotels. | Passed |
| **2.2** | 8 | The user clicks on the "Tatkal Deals" filter on the Home Page. | The list updates to display only hotels where the last-minute `tatkalPrice` is lower than the regular `price`. | The list updates correctly to show available last-minute deals. | Passed |
| **2.3** | 9 | The user clicks on the "All" filter after browsing a specific category. | The hotel list resets and displays all hotels fetched from the database without any category restrictions. | The list resets correctly to show all available hotels. | Passed |
| **3. Hotel Details & Booking Flow** | | | | | |
| **3.1** | 10 | The user clicks on a specific Hotel card to view details. | The application navigates to the Hotel Details page, successfully fetching and displaying the hotel's amenities, location, and associated rooms from the database. | The Hotel Details page renders correctly with the specific location, amenities, and room choices fetched from the database. | Passed |
| **3.2** | 11 | The user attempts to select a hotel that has `availability: 0` (Sold Out). | The "Book Now" button on the Home Page is disabled and visually greyed out, preventing navigation or indicating it cannot be booked. | The button is correctly disabled and displays "Sold Out". | Passed |
| **3.3** | 12 | On the Hotel Details page, the user clicks the "⚡ Fast-Track: Apply Tatkal (Today)" button. | The check-in date input is automatically populated with today's date, and the room prices dynamically update to reflect the cheaper Tatkal rates. | The date updates immediately and the discounted prices are applied to the UI displaying the "TATKAL DEAL" tag. | Passed |
| **3.4** | 13 | The user selects a room and a date, and presses the "Confirm Booking" button while NOT logged into the system. | A warning alert is displayed prompting the user to log in, and the user is redirected to the `/login` route. | The alert is correctly triggered and the navigation redirects the user to the login screen. | Passed |
| **3.5** | 14 | The user presses "Confirm Booking" without selecting a check-in date. | An error message "Please select dates and a room" is displayed to the user, preventing further action. | The error message is successfully displayed to the user. | Passed |
| **3.6** | 15 | The user presses "Confirm Booking" without selecting a specific room type from the list. | An error message "Please select dates and a room" is displayed to the user, preventing the booking confirmation. | The error message is successfully displayed to the user. | Passed |
| **3.7** | 16 | The user selects a check-in date in the past (e.g., yesterday). | The system should display an error and prevent the booking from completing since the date is invalid. | The system allows the user to proceed to the payment page with a past date. | Failed |
| **3.8** | 17 | The logged-in user successfully completes a booking with valid dates and room selection. | The application navigates the user to the `/payment` screen to process the transaction securely. | The user is redirected to the dummy payment page with correct booking details. | Passed |
| **4. Packages Flow** | | | | | |
| **4.1** | 18 | The user navigates to the Curated Packages screen (`/packages`). | A grid of custom travel packages is fetched from the database and displayed with their respective inclusions and prices. | Packages are fetched and rendered properly without errors. | Passed |
| **4.2** | 19 | The logged-in user clicks "Book Package" on the Packages screen. | The application redirects the user to the `/payment` screen passing the package details securely. | The navigation correctly routes to the payment gateway dummy page. | Passed |
| **5. Payment Gateway** | | | | | |
| **5.1** | 20 | The user views the Payment Screen after a successful booking step. | A secure payment form is rendered displaying the total amount. User can toggle between Credit Card and UPI input methods. | The Payment screen correctly displays inputs and total booking amount. | Passed |
| **5.2** | 21 | The user attempts to submit the Payment form with a 2-digit CVV. | The system should enforce a minimum length of 3 digits for the CVV and prevent submission. | The CVV field has `maxLength="4"` but no `minLength`, allowing submission of a 2-digit CVV. | Failed |
| **5.3** | 22 | The user fills out valid payment details and clicks "Pay". | The system simulates a process delay, logs the booking in the global context, and navigates to the `/invoice` screen. | The booking is successfully added and invoice loads immediately after payment delay. | Passed |
| **6. Invoice & Dashboard** | | | | | |
| **6.1** | 23 | The user views the Invoice Screen after a successful payment. | A formatted bill is displayed showing the booking details, room pricing (including Tatkal discounts if applicable), taxes ($45), and the correct total amount. | The invoice mathematically correctly adds the room price and standard fees. | Passed |
| **6.2** | 24 | A logged-in user clicks "Dashboard" on the navigation bar. | The application navigates to `/dashboard`, displaying the user's profile information and a list of their current bookings. | Dashboard UI renders with active session details and booking array correctly filtered. | Passed |
| **6.3** | 25 | A logged-in user clicks "Cancel Booking" for an active booking in the Dashboard. | The status of the booking updates to "Cancelled" and is visually represented in red text to indicate the change. | The booking is successfully marked as cancelled via AppContext. | Passed |
| **7. Error Handling** | | | | | |
| **7.1** | 26 | The user navigates directly to a non-existent hotel ID via URL (e.g. `/hotel/9999`). | The system gracefully handles the "not found" state and displays "Hotel not found" text instead of crashing. | The empty state handles the null fetch response successfully. | Passed |
